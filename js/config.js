// 공개 저장소 정보 (읽기 전용, 비밀값 아님). 저장소를 만든 뒤 아래 값을 실제 값으로 바꾸세요.
// api/*.js가 사용하는 서버 환경변수(.env)와 반드시 같은 값이어야 합니다.
window.APP_CONFIG = {
  githubOwner: 'skt1109735-sys',
  githubRepo: 'meeting-availability',
  githubBranch: 'main',
};
